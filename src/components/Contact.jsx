function Contact() {
  return (
    <section className="section-card" id="contact" data-reveal>
      <div className="contact-box">
        <div>
          <h2 data-reveal>Let’s create something exceptional.</h2>
          <p className="section-intro" data-reveal>
            Ready to launch a brand experience that feels refined from the first click?
          </p>
        </div>
        <a href="mailto:hello@codimp.com" className="btn-primary" data-reveal>
          Get In Touch
        </a>
      </div>
    </section>
  )
}

export default Contact

